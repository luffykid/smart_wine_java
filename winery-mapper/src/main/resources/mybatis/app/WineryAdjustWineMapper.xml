<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.changfa.frame.mapper.app.WineryAdjustWineMapper" >
    <resultMap id="wineryAdjustWineMap" type="com.changfa.frame.model.app.WineryAdjustWine" >
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="adjust_name" property="adjustName" jdbcType="VARCHAR" />
        <result column="winery_id" property="wineryId" jdbcType="BIGINT" />
        <result column="total_seller_cnt" property="totalSellerCnt" jdbcType="INTEGER" />
        <result column="create_date" property="createDate" jdbcType="TIMESTAMP" />
        <result column="modify_date" property="modifyDate" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="table_column" >
        waw.id as id, waw.adjust_name as adjust_name, waw.winery_id as winery_id, waw.total_seller_cnt as total_seller_cnt, 
        waw.create_date as create_date, waw.modify_date as modify_date
    </sql>

    <select id="getById" resultMap="wineryAdjustWineMap" parameterType="java.lang.Long" >
        select 
        <include refid="table_column" />
        from b_winery_adjust_wine waw
        where waw.id = #{id}
    </select>

    <insert id="save" parameterType="com.changfa.frame.model.app.WineryAdjustWine" >
        insert into b_winery_adjust_wine (id, adjust_name, winery_id, total_seller_cnt, create_date, modify_date
            )
        values (#{id}, #{adjustName}, #{wineryId}, #{totalSellerCnt}, #{createDate}, #{modifyDate}
            )
    </insert>

    <update id="update" parameterType="com.changfa.frame.model.app.WineryAdjustWine" >
        update b_winery_adjust_wine
        <set >
            <if test="adjustName != null and adjustName != ''" >
                adjust_name = #{adjustName},
            </if>
            <if test="wineryId != null" >
                winery_id = #{wineryId},
            </if>
            <if test="totalSellerCnt != null" >
                total_seller_cnt = #{totalSellerCnt},
            </if>
            <if test="createDate != null" >
                create_date = #{createDate},
            </if>
            <if test="modifyDate != null" >
                modify_date = #{modifyDate},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long" >
        delete from b_winery_adjust_wine
        where id = #{id}
    </delete>

    <select id="selectList" resultMap="wineryAdjustWineMap" >
        select 
        <include refid="table_column" />
        from b_winery_adjust_wine waw
        <where >
            <if test="adjustName != null and adjustName != ''" >
                and waw.adjust_name = #{adjustName}
            </if>
            <if test="wineryId != null" >
                and waw.winery_id = #{wineryId}
            </if>
            <if test="totalSellerCnt != null" >
                and waw.total_seller_cnt = #{totalSellerCnt}
            </if>
            <if test="createDate != null" >
                and waw.create_date = #{createDate}
            </if>
            <if test="modifyDate != null" >
                and waw.modify_date = #{modifyDate}
            </if>
        </where>
    </select>

</mapper>