<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.changfa.frame.mapper.app.WineryActivityLikeMapper" >
    <resultMap id="wineryActivityLikeMap" type="com.changfa.frame.model.app.WineryActivityLike" >
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="winery_activity_id" property="wineryActivityId" jdbcType="BIGINT" />
        <result column="mbr_id" property="mbrId" jdbcType="BIGINT" />
        <result column="winery_id" property="wineryId" jdbcType="BIGINT" />
        <result column="like_status" property="likeStatus" jdbcType="INTEGER" />
        <result column="create_date" property="createDate" jdbcType="TIMESTAMP" />
        <result column="modify_date" property="modifyDate" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="table_column" >
        l.id as id, l.winery_activity_id as winery_activity_id, l.mbr_id as mbr_id, l.winery_id as winery_id, 
        l.like_status as like_status, l.create_date as create_date, l.modify_date as modify_date
    </sql>

    <select id="getById" resultMap="wineryActivityLikeMap" parameterType="java.lang.Long" >
        select 
        <include refid="table_column" />
        from b_winery_activity_like l
        where l.id = #{id}
    </select>

    <insert id="save" parameterType="com.changfa.frame.model.app.WineryActivityLike" >
        insert into b_winery_activity_like (id, winery_activity_id, mbr_id, winery_id, like_status, create_date, 
            modify_date)
        values (#{id}, #{wineryActivityId}, #{mbrId}, #{wineryId}, #{likeStatus}, #{createDate}, 
            #{modifyDate})
    </insert>

    <update id="update" parameterType="com.changfa.frame.model.app.WineryActivityLike" >
        update b_winery_activity_like
        <set >
            <if test="wineryActivityId != null" >
                winery_activity_id = #{wineryActivityId},
            </if>
            <if test="mbrId != null" >
                mbr_id = #{mbrId},
            </if>
            <if test="wineryId != null" >
                winery_id = #{wineryId},
            </if>
            <if test="likeStatus != null" >
                like_status = #{likeStatus},
            </if>
            <if test="createDate != null" >
                create_date = #{createDate},
            </if>
            <if test="modifyDate != null" >
                modify_date = #{modifyDate},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long" >
        delete from b_winery_activity_like
        where id = #{id}
    </delete>

    <select id="selectList" resultMap="wineryActivityLikeMap" >
        select 
        <include refid="table_column" />
        from b_winery_activity_like l
        <where >
            <if test="wineryActivityId != null" >
                and l.winery_activity_id = #{wineryActivityId}
            </if>
            <if test="mbrId != null" >
                and l.mbr_id = #{mbrId}
            </if>
            <if test="wineryId != null" >
                and l.winery_id = #{wineryId}
            </if>
            <if test="likeStatus != null" >
                and l.like_status = #{likeStatus}
            </if>
            <if test="createDate != null" >
                and l.create_date = #{createDate}
            </if>
            <if test="modifyDate != null" >
                and l.modify_date = #{modifyDate}
            </if>
        </where>
    </select>

</mapper>